apiVersion: v1
kind: Service
metadata:
  name: search-rpc
spec:
  selector:
    app: search-rpc
  ports:
    - name: grpc
      port: 8088
      targetPort: 8088
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-rpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-rpc
  template:
    metadata:
      labels:
        app: search-rpc
    spec:
      containers:
        - name: search-rpc
          image: schill-search-rpc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8088
          readinessProbe:
            tcpSocket:
              port: 8088
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8088
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: config
              mountPath: /app/etc
              readOnly: true
      volumes:
        - name: config
          secret:
            secretName: schill-app-config
