# SChill 功能流程图（图片版）

## 目录
1. [项目架构概述](#项目架构概述)
2. [数据库表关系](#数据库表关系)
3. [用户服务接口流程](#用户服务接口流程)
4. [内容服务接口流程](#内容服务接口流程)
5. [评论服务接口流程](#评论服务接口流程)
6. [关系服务接口流程](#关系服务接口流程)
7. [Kafka 消息流程](#kafka-消息流程)

---

## 如何查看和导出图片

### 方式1：使用 VS Code 插件
1. 安装 "Markdown Preview Mermaid Support" 插件
2. 在 VS Code 中打开此文件
3. 右键选择 "Markdown: Open Preview to the Side"
4. 右键预览中的图表 → "Export to SVG" 或 "Export to PNG"

### 方式2：使用在线工具
访问 https://mermaid.live/，将每个图表的代码粘贴进去，即可预览和下载图片。

---

## 项目架构概述

### 整体架构图

```mermaid
graph TB
    Frontend[前端 Vue 3 + TS]
    API[API 服务层 Go-Zero]
    RPC[RPC 服务层 Go-Zero]
    MySQL[(MySQL 主数据库)]
    Etcd[(Etcd 服务发现)]
    Kafka[(Kafka 消息队列)]
    
    Frontend -->|HTTP/REST| API
    API -->|gRPC| RPC
    RPC --> MySQL
    RPC --> Etcd
    RPC --> Kafka
    
    subgraph API层
        UserAPI[User API]
        ContentAPI[Content API]
        CommentAPI[Comment API]
        RelationAPI[Relation API]
    end
    
    subgraph RPC层
        UserRPC[User RPC]
        ContentRPC[Content RPC]
        CommentRPC[Comment RPC]
        RelationRPC[Relation RPC]
    end
    
    API --> UserAPI
    API --> ContentAPI
    API --> CommentAPI
    API --> RelationAPI
    
    RPC --> UserRPC
    RPC --> ContentRPC
    RPC --> CommentRPC
    RPC --> RelationRPC
```

---

## 数据库表关系

### ER 图

```mermaid
erDiagram
    user ||--|| user_profile : has
    user ||--|| user_stat : has
    user ||--o{ post : creates
    user ||--o{ comment : writes
    user ||--o{ following : follows
    
    post ||--o{ post_content : has
    post ||--o{ post_topic : tagged_with
    post ||--o{ comment : has
    
    topic ||--o{ post_topic : tagged_in
    
    comment ||--|| comment_content : has
    comment ||--o{ comment_vote : receives
    
    following ||--o{ user : follower
    
    user {
        bigint id PK
        varchar username
        varchar password_hash
        varchar avatar
        tinyint status
        tinyint is_admin
        varchar last_login_ip
        timestamp last_login_time
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    
    user_profile {
        bigint id PK
        bigint user_id FK
        tinyint gender
        date birthday
        varchar signature
        varchar location
        varchar website
        varchar company
        varchar job_title
        varchar education
    }
    
    user_stat {
        bigint id PK
        bigint user_id FK
        int post_count
        int comment_count
        int follower_count
        int following_count
        int like_count
        int collection_count
        bigint last_active_time
    }
    
    topic {
        bigint id PK
        varchar name
        bigint quote_num
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    
    post {
        bigint id PK
        bigint user_id FK
        bigint comment_count
        bigint collection_count
        bigint upvote_count
        bigint share_count
        tinyint visibility
        tinyint is_top
        tinyint is_essence
        tinyint is_lock
        bigint latest_replied_at
        varchar tags
        varchar ip
        varchar ip_loc
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    
    post_content {
        bigint id PK
        bigint post_id FK
        bigint user_id FK
        text content
        tinyint type
        int sort
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    
    post_topic {
        bigint id PK
        bigint post_id FK
        bigint topic_id FK
        timestamp created_at
    }
    
    comment {
        bigint id PK
        bigint group_id
        bigint user_id FK
        bigint parent_id
        bigint reply_to_user_id
        tinyint level
        tinyint status
        int reply_count
        int like_count
        int dislike_count
        varchar ip
        varchar ip_loc
        datetime created_at
        datetime updated_at
        datetime deleted_at
    }
    
    comment_content {
        bigint id PK
        bigint comment_id FK
        mediumtext content
        tinyint content_type
        datetime created_at
        datetime updated_at
    }
    
    comment_vote {
        bigint id PK
        bigint comment_id FK
        bigint user_id FK
        tinyint vote_type
        datetime created_at
        datetime updated_at
    }
    
    following {
        bigint id PK
        bigint user_id FK
        bigint follow_id FK
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
```

---

## 用户服务接口流程

### 1. 用户注册 (Register)

**文件位置**: `service/user/rpc/internal/logic/registerlogic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> CheckUsername{检查用户名是否存在}
    CheckUsername -->|存在| Err1[返回错误: 用户名已存在]
    CheckUsername -->|不存在| Encrypt[密码加密 bcrypt]
    Encrypt --> BeginTx[开启事务]
    BeginTx --> CreateUser[创建 user 记录]
    CreateUser --> CreateProfile[创建 user_profile 记录]
    CreateProfile --> CreateStat[创建 user_stat 记录]
    CreateStat --> CommitTx[提交事务]
    CommitTx --> Return[返回 user_id]
    
    style Err1 fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `RegisterReq { Username, Password }`
- 输出: `RegisterResp { UserId }`
- 数据库写入: user, user_profile, user_stat

---

### 2. 用户登录 (Login)

**文件位置**: `service/user/rpc/internal/logic/loginlogic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> QueryUser[查询用户 by username]
    QueryUser --> Exist1{用户存在?}
    Exist1 -->|否| Err1[返回错误: 凭证无效]
    Exist1 -->|是| CheckDeleted{检查软删除}
    CheckDeleted -->|已删除| Err1
    CheckDeleted -->|未删除| VerifyPassword{验证密码}
    VerifyPassword -->|错误| Err1
    VerifyPassword -->|正确| CheckStatus{检查状态 status==1?}
    CheckStatus -->|不正常| Err2[返回错误: 账户异常]
    CheckStatus -->|正常| UpdateLogin[更新登录时间和IP]
    UpdateLogin --> Return[返回 user_id]
    
    style Err1 fill:#ff6b6b
    style Err2 fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `LoginReq { Username, Password }`
- 输出: `LoginResp { UserId }`
- 数据库读取: user 表
- 数据库更新: user.last_login_time, user.last_login_ip

---

### 3. 获取用户统计 (GetUserStat)

**文件位置**: `service/user/rpc/internal/logic/getuserstatlogic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> QueryStat[查询 user_stat 表]
    QueryStat --> Exist{存在?}
    Exist -->|否| Err[返回错误: 用户不存在]
    Exist -->|是| Assemble[组装响应 映射字段]
    Assemble --> Return[返回统计信息]
    
    style Err fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `GetUserStatReq { UserId }`
- 输出: `GetUserStatResp { Stat { UserId, PostCount, CommentCount, FollowerCount, FollowingCount, LikeCount, CollectionCount, LastActiveTime } }`

---

## 内容服务接口流程

### 1. 创建帖子 (CreatePost)

**文件位置**: `service/content/rpc/internal/logic/create_post_logic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> Validate{参数校验<br/>title/contents不为空}
    Validate -->|不通过| Err1[返回错误]
    Validate -->|通过| BeginTx[开启事务]
    BeginTx --> CreatePost[创建 post 主记录]
    CreatePost --> CreateTitle[创建标题 content type=1]
    CreateTitle --> HasCover{有封面?}
    HasCover -->|是| CreateCover[创建封面 content type=3]
    HasCover -->|否| CreateContents
    CreateCover --> CreateContents[遍历创建正文内容 type=2]
    CreateContents --> HasTopics{有话题?}
    HasTopics -->|是| ProcessTopics[处理话题]
    HasTopics -->|否| CommitTx
    
    subgraph 话题处理
        ProcessTopics --> ForEachTopic[遍历每个话题]
        ForEachTopic --> TopicExist{话题已存在?}
        TopicExist -->|是| IncQuote[quote_num + 1]
        TopicExist -->|否| CreateTopic[创建新话题 quote_num=1]
        IncQuote --> CreatePostTopic
        CreateTopic --> CreatePostTopic[创建 post_topic 关联]
    end
    
    CreatePostTopic --> CommitTx[提交事务]
    CommitTx --> QueryNewPost[查询新帖子ID]
    QueryNewPost --> BuildMsg[构造Kafka消息 {user_id, post_id}]
    BuildMsg --> SendKafka[发送到 post-created 主题]
    SendKafka --> Return[返回 post_id]
    
    style Err1 fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `CreatePostReq { UserId, Title, Cover, Contents [{Type, Content, Sort}], Topics [], Visibility, Tags }`
- 输出: `CreatePostResp { PostId }`
- 数据库写入（事务内）: post, post_content, topic, post_topic
- Kafka 消息: 发送到 `post-created` 主题

---

### 2. 删除帖子 (DeletePost)

**文件位置**: `service/content/rpc/internal/logic/delete_post_logic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> QueryPost[查询帖子]
    QueryPost --> Exist{帖子存在?}
    Exist -->|否| Err1[返回错误: 帖子不存在]
    Exist -->|是| CheckPermission{权限校验<br/>post.user_id == user_id?}
    CheckPermission -->|否| Err2[返回错误: 无权限]
    CheckPermission -->|是| BeginTx[开启事务]
    
    BeginTx --> QueryPostTopics[查询关联的 post_topic]
    QueryPostTopics --> ForEachPT[遍历减少 topic.quote_num]
    ForEachPT --> DeletePT[删除 post_topic 记录]
    DeletePT --> DeleteContent[删除 post_content 记录]
    DeleteContent --> DeletePost[删除 post 记录]
    DeletePost --> CommitTx[提交事务]
    
    CommitTx --> BuildMsg[构造Kafka消息 {user_id, post_id}]
    BuildMsg --> SendKafka[发送到 post-deleted 主题]
    SendKafka --> Return[返回成功]
    
    style Err1 fill:#ff6b6b
    style Err2 fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `DeletePostReq { PostId, UserId }`
- 输出: `DeletePostResp { Success }`
- 数据库操作（事务内）: 更新 topic.quote_num, 删除 post_topic/post_content/post
- Kafka 消息: 发送到 `post-deleted` 主题

---

### 3. 获取帖子详情 (GetPostDetail)

**文件位置**: `service/content/rpc/internal/logic/get_post_detail_logic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> QueryPost[查询 post 主记录]
    QueryPost --> Exist{存在?}
    Exist -->|否| Err[返回错误: 帖子不存在]
    Exist -->|是| QueryContent[查询 post_content 按 sort 排序]
    QueryContent --> QueryTopics[查询 post_topic 关联记录]
    QueryTopics --> ExtractTopicIds[提取 topic_id 列表]
    ExtractTopicIds --> QueryTopicDetails[查询 topic 详情]
    QueryTopicDetails --> BuildTopicMap[构建 topic name map]
    BuildTopicMap --> ParseContent[解析内容:<br/>type=1→title<br/>type=3→cover<br/>其他→contents]
    ParseContent --> Assemble[组装 PostInfo 和响应]
    Assemble --> Return[返回详情]
    
    style Err fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `GetPostDetailReq { PostId }`
- 输出: `GetPostDetailResp { Post {}, Contents [], Topics [] }`

---

### 4. 获取帖子列表 (GetPostList)

**文件位置**: `service/content/rpc/internal/logic/get_post_list_logic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> ProcessParams[参数处理:<br/>page默认=1<br/>page_size默认=20最大=100]
    ProcessParams --> CalcOffset[计算 offset]
    CalcOffset --> BuildQuery[构建查询 可选 user_id]
    BuildQuery --> CountTotal[COUNT 统计总数]
    CountTotal --> QueryPosts[查询 posts 分页+排序]
    QueryPosts --> ExtractPostIds[提取 post_id 列表]
    ExtractPostIds --> BatchQueryContent[批量查询 post_content]
    BatchQueryContent --> BuildContentMap[构建内容 map title, cover]
    BuildContentMap --> AssembleList[组装列表响应]
    AssembleList --> Return[返回列表]
    
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `GetPostListReq { Page, PageSize, UserId }`
- 输出: `GetPostListResp { Total, List [] }`

---

## 评论服务接口流程

### 1. 创建评论 (CreateComment) - 待实现

**文件位置**: `service/comment/rpc/internal/logic/createcommentlogic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> Validate[校验参数]
    Validate --> BeginTx[开启事务]
    BeginTx --> CreateComment[创建 comment 记录]
    CreateComment --> CreateContent[创建 comment_content 记录]
    CreateContent --> IsReply{是回复?}
    IsReply -->|是| IncReplyCount[父评论 reply_count + 1]
    IsReply -->|否| CommitTx
    IncReplyCount --> CommitTx[提交事务]
    CommitTx --> Return[返回评论ID]
    
    style Return fill:#51cf66
```

**预计数据转换**:
- 输入: `CreateCommentReq { GroupId, UserId, Content, ParentId, ReplyToUserId }`
- 输出: `CreateCommentResp { CommentId }`
- 数据库写入（事务内）: comment, comment_content

---

## 关系服务接口流程

### 1. 关注用户 (Follow)

**文件位置**: `service/relation/rpc/internal/logic/followlogic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> CheckSelf{检查是否关注自己}
    CheckSelf -->|是| Err1[返回错误: 不能关注自己]
    CheckSelf -->|否| QueryExist[查询是否已经关注]
    QueryExist --> AlreadyFollowed{已关注?}
    AlreadyFollowed -->|是| Err2[返回错误: 已关注]
    AlreadyFollowed -->|否| CreateFollowing[创建关注记录]
    CreateFollowing --> Return[返回成功]
    
    style Err1 fill:#ff6b6b
    style Err2 fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `FollowReq { UserId, TargetUserId }`
- 输出: `FollowResp { Success }`
- 数据库写入: `following { user_id, follow_id }`

---

### 2. 取消关注 (Unfollow)

**文件位置**: `service/relation/rpc/internal/logic/unfollowlogic.go`

```mermaid
flowchart TD
    Start([接收请求]) --> QueryFollowing[查询关注记录]
    QueryFollowing --> Exist{存在?}
    Exist -->|否| Err[返回错误: 未关注]
    Exist -->|是| DeleteFollowing[删除关注记录]
    DeleteFollowing --> Return[返回成功]
    
    style Err fill:#ff6b6b
    style Return fill:#51cf66
```

**数据转换**:
- 输入: `UnfollowReq { UserId, TargetUserId }`
- 输出: `UnfollowResp { Success }`
- 数据库删除: `following` 记录

---

## Kafka 消息流程

### 整体消息流架构

```mermaid
graph TB
    subgraph ContentRPC[Content RPC 服务]
        CreatePost[CreatePostLogic<br/>- 创建帖子<br/>- 发送 post-created]
        DeletePost[DeletePostLogic<br/>- 删除帖子<br/>- 发送 post-deleted]
    end
    
    subgraph Kafka[Kafka Cluster KRaft]
        TopicCreated[Topic: post-created]
        TopicDeleted[Topic: post-deleted]
    end
    
    subgraph UserRPC[User RPC 服务]
        PostCreatedConsumer[PostCreatedConsumer<br/>- 消费 post-created<br/>- user_stat.post_count +=1]
        PostDeletedConsumer[PostDeletedConsumer<br/>- 消费 post-deleted<br/>- user_stat.post_count -=1]
    end
    
    CreatePost -->|发送消息| TopicCreated
    DeletePost -->|发送消息| TopicDeleted
    
    TopicCreated -->|消费| PostCreatedConsumer
    TopicDeleted -->|消费| PostDeletedConsumer
```

---

### 1. Post-Created 消息消费流程

**文件位置**: `service/user/rpc/internal/mqs/postcreated.go`

```mermaid
flowchart TD
    Start([Kafka Consumer 接收消息]) --> ParseJSON[解析 JSON {user_id, post_id}]
    ParseJSON --> BeginTx[开启事务]
    BeginTx --> QueryStat[查询 user_stat]
    QueryStat --> Exist{存在?}
    Exist -->|否| CreateNew[创建新记录 post_count=1]
    Exist -->|是| IncCount[post_count +=1]
    CreateNew --> CommitTx
    IncCount --> CommitTx[提交事务]
    CommitTx --> MarkOffset[Mark Offset]
    
    style MarkOffset fill:#51cf66
```

**数据转换**:
- Kafka 消息: `{ user_id, post_id }`
- 数据库操作: `user_stat.post_count += 1` (事务)

---

### 2. Post-Deleted 消息消费流程

**文件位置**: `service/user/rpc/internal/mqs/postdeleted.go`

```mermaid
flowchart TD
    Start([Kafka Consumer 接收消息]) --> ParseJSON[解析 JSON {user_id, post_id}]
    ParseJSON --> BeginTx[开启事务]
    BeginTx --> QueryStat[查询 user_stat]
    QueryStat --> Exist{存在?}
    Exist -->|否| CreateNew[创建新记录 post_count=0]
    Exist -->|是| CheckGT0{post_count > 0?}
    CreateNew --> CommitTx
    CheckGT0 -->|是| DecCount[post_count -=1]
    CheckGT0 -->|否| Skip[跳过 已为0]
    DecCount --> CommitTx
    Skip --> CommitTx[提交事务]
    CommitTx --> MarkOffset[Mark Offset]
    
    style MarkOffset fill:#51cf66
```

**数据转换**:
- Kafka 消息: `{ user_id, post_id }`
- 数据库操作: `user_stat.post_count -= 1` (仅当 post_count > 0 时，事务)

---

## 总结

### 关键设计模式

| 模式 | 说明 |
|------|------|
| API + RPC 双层架构 | API 层负责 REST 接口，RPC 层负责业务逻辑和数据库操作 |
| 数据库事务 | 涉及多表操作的接口都使用事务保证一致性 |
| Kafka 事件驱动 | 帖子创建/删除通过 Kafka 异步更新用户统计 |
| 软删除 | 核心表都使用 `deleted_at` 实现软删除 |

### 事务使用场景

| 接口 | 涉及表 | 原因 |
|------|--------|------|
| Register | user, user_profile, user_stat | 必须同时创建三个表记录 |
| CreatePost | post, post_content, topic, post_topic | 帖子内容、话题关联必须一致 |
| DeletePost | topic, post_topic, post_content, post | 删除操作必须按顺序回滚 |
| PostCreatedConsumer | user_stat | 计数更新需要事务保护 |
| PostDeletedConsumer | user_stat | 计数更新需要事务保护 |

### Kafka 主题

| 主题 | 生产者 | 消费者 | 用途 |
|------|--------|--------|------|
| post-created | Content RPC | User RPC | 帖子创建，增加发帖数 |
| post-deleted | Content RPC | User RPC | 帖子删除，减少发帖数 |
