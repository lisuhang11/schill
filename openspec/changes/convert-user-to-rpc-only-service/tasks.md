# Tasks: Convert User Service To RPC-Only Boundary

## 1. Redesign RPC contract

- [x] Update `service/user/rpc/user.proto` so `LoginResp` includes access and refresh token fields.
- [x] Add a `RefreshToken` RPC method with request and response messages.
- [x] Make authenticated mutations carry an explicit authenticated `userId`.
- [x] Keep avatar mutation URL-based rather than file-bytes-based.
- [x] Regenerate go-zero RPC code from the updated proto.

## 2. Move session logic into user-rpc

- [x] Add JWT configuration to `service/user/rpc/internal/config/config.go`.
- [x] Add JWT values to `service/user/rpc/etc/user-rpc.yaml` and deployment config files.
- [x] Update login logic to generate access and refresh tokens after credential verification.
- [x] Add refresh-token logic that parses refresh tokens and returns a new token pair.
- [x] Verify refresh rejects invalid, expired, deleted-user, or abnormal-status sessions.

## 3. Align user-rpc mutations and reads

- [x] Ensure profile update logic uses authenticated `userId` from the RPC request.
- [x] Preserve profile patch field presence semantics if optional profile-field work is applied.
- [x] Ensure avatar URL update validates user existence and invalidates user caches.
- [x] Confirm batch user lookup remains compatible with relation, feed, and comment callers.

## 4. Update callers and deployment boundaries

- [x] Inventory all references to `user-api`, `/user/*`, and user-rpc generated client types.
- [x] Update internal RPC callers for any proto message or method changes.
- [x] Decide and document the replacement HTTP adapter path before removing user-api runtime routes.
- [x] Remove `service/user/api` build and deployment references only after replacement behavior is available.
- [x] Update gateway configuration that currently targets `http://user-api:8888`.

## 5. Verify

- [x] Build `service/user/rpc`.
- [ ] Run user-rpc unit tests or add focused tests for login and refresh-token behavior.
- [x] Run affected service builds for relation, feed, comment, and any user-rpc callers.
- [x] Verify token generated by user-rpc is accepted by gateway access-token validation.
- [ ] Verify avatar URL updates still persist and invalidate user caches.
